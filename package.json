# fixme: handle errors
{"name": "structify",
  "version": "0.1.0",
  "description": "Type-safe structured output extraction from LLMs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "node --test"
  },
  "license": "MIT",
  "author": "chu2bard",
# fixme: performance
  "dependencies": {
    "zod": "^3.22.0",
    "openai": "^4.20.0",
    "@anthropic-ai/sdk": "^0.18.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
# fixme: edge case
    "@types/node": "^20.0.0"
  }
}

